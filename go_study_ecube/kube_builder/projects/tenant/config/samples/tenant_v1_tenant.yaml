apiVersion: tenant.tenant.haulla.com/v1
kind: Tenant
metadata:
  labels:
    app.kubernetes.io/name: tenant
    app.kubernetes.io/managed-by: kustomize
  name: tenant-sample
spec:
  # TODO(user): Add fields here
